import React from 'react';

// Deprecated: SessionBadge is no longer used. Session management now lives in the RAZ tab.
export const SessionBadge: React.FC = () => {
  return null;
};

export default SessionBadge;
